version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - DATABASE_URL=sqlite:///./app.db
      - SECRET_KEY=replace_this_with_a_secure_random_string
    depends_on: []
